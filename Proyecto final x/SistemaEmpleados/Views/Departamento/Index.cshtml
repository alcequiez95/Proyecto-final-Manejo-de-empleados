@model IEnumerable<SistemaEmpleados.Models.Departamento>

@{
    ViewBag.Title = "Departamentos - Sistema de Manejo de Empleados";
    Layout = "~/Views/Shared/_Layout.cshtml";
}

<div class="header">
    <h1>Gestión de Departamentos</h1>
</div>

<div class="btn-group">
    <a href="/Departamento/Create" class="btn btn-primary">+ Agregar Nuevo Departamento</a>
    <a href="/Departamento/Exportar" class="btn btn-secondary">📥 Exportar a CSV</a>
</div>

@if (Model.Count() == 0)
{
    <div class="alert alert-warning">
        No hay departamentos registrados. <a href="/Departamento/Create">Crear el primer departamento</a>
    </div>
}
else
{
    <table>
        <thead>
            <tr>
                <th>ID</th>
                <th>Nombre</th>
                <th>Descripción</th>
                <th>Fecha Creación</th>
                <th>Acciones</th>
            </tr>
        </thead>
        <tbody>
            @foreach (var departamento in Model)
            {
                <tr>
                    <td>@departamento.DepartamentoID</td>
                    <td>@departamento.Nombre</td>
                    <td>@(string.IsNullOrEmpty(departamento.Descripcion) ? "Sin descripción" : departamento.Descripcion)</td>
                    <td>@departamento.FechaCreacion.ToString("dd/MM/yyyy")</td>
                    <td class="acciones">
                        <a href="/Departamento/Edit/@departamento.DepartamentoID" class="btn btn-warning btn-small">Editar</a>
                        <a href="/Departamento/Delete/@departamento.DepartamentoID" class="btn btn-danger btn-small">Eliminar</a>
                    </td>
                </tr>
            }
        </tbody>
    </table>
}
